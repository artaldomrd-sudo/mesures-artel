# Repo limpio: por defecto ignorar TODO lo de esta carpeta...
/*

# ...y volver a incluir SOLO los archivos del código del app:
!index.html
!logo.png
!CLAUDE.md
!verify.mjs
!README.md
!.gitignore

# (Así git no versiona: fotos HEIC, RTF, copias .json, subcarpetas de
#  cotización/fabricación, .DS_Store, archivos temporales, node_modules, etc.)
